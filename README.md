# LadTest

Для запуска поднимите базу данных `docker-compose up -d`. 

Установите зависимости `pip install -r requirements.txt`. 

Установите миграции `alembic upgrade head`.

Внесите данные в базу и запустите файл `main.py`.